#include<stdio.h>
int main()
{
    int result=pow(-50,2);  // we can use this program to define "abs" "sqrt" "pow"
                                //in this programs we have to use intejer data type

    printf("%d",result);
    getch();
}
